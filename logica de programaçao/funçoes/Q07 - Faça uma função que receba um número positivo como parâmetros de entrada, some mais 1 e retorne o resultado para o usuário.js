// Faça uma função que receba um número positivo como parâmetros de entrada, some mais 1 e retorne o resultado para o usuário.

function parametro(numero) {
    return numero + 1; 
}

var resultado = parametro(10);
console.log(resultado);